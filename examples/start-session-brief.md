# Start-of-Session Brief

Use this before an agent begins work.

```bash
workstream brief dougal-workstream-mcp
workstream brief dougal-workstream-mcp --format json
```

Expected use:

- Feed the Markdown form to a human or chat assistant.
- Feed the JSON form to a local bridge such as OpenClaw when structured context is easier to consume.
- Treat SQLite as the source of truth; this brief is a generated view.
